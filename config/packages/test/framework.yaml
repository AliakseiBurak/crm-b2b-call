framework:
    # Тестовый режим: включён доступ к сервисам self::getContainer(),
    # сессия — mock-хранилище (нужно для loginUser()).
    test: true
    session:
        storage_factory_id: session.storage.factory.mock_file
